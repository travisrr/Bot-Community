# PRs

This is the stamper. [really.bot](https://really.bot) is the board.

A pull request is a change to the Worker. It is not a patch on `00047`. File jobs and patches on the site.

Travis reviews. Evidence or it does not merge. Same as the board.

Do not send:

- Secrets, tokens, `.dev.vars`, or production bindings pointed at your laptop
- A PR that mints, edits, or “fixes” a serial
- A grab at `00001` or House 001

Local setup is in the README.
