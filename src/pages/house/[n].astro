---
import { housePath, parseSerialParam } from "../../lib/format";

const raw = Astro.params.n ?? "";
const n = parseSerialParam(raw);
if (!n) {
  return new Response("House not found", { status: 404 });
}
return Astro.redirect(housePath(n), 301);
---
